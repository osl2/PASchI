/*import {IModelDtoMapper} from "@/dto/mapper/IModelDtoMapper";
import {User} from "@/model/User";
import {UserDto} from "@/dto/UserDto";

export class UserMapper implements IModelDtoMapper<User, UserDto> {

  modelToDto(user: User): UserDto {

    return new UserDto(user.getId, user.firstName, user.lastName, user.email, user.auth, user.role, user.token);
  }

  dtoToModel(d: UserDto): User {
  }
}*/
